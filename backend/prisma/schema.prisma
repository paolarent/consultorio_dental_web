generator client {
  provider = "prisma-client-js"
  output   = "../generated/prisma"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model consultorio {
  id_consultorio Int      @id
  nombre         String   @db.VarChar(100)
  descripcion    String?
  fecha_registro DateTime @db.Date
  telefono       String   @db.VarChar(10)
  correo         String   @db.VarChar(60)
  d_calle        String?  @db.VarChar(30)
  d_num_exterior String?  @db.VarChar(5)
  d_colonia      String?  @db.VarChar(30)
  d_cp           String?  @db.VarChar(5)
  d_entidadfed   String?  @db.VarChar(30)
  d_municipio    String?  @db.VarChar(30)
  d_localidad    String?  @db.VarChar(30)
  titular_ap1    String   @db.VarChar(30)
  titular_ap2    String   @db.VarChar(30)
  titular_nombre String   @db.VarChar(50)
  status         status   @default(activo)
}

enum status { 
  activo
  inactivo
}

enum status_citas {
  pendiente
  confirmada
  cancelada
  completada
  reprogramada
}

enum status_cita_reprog {
  confirmada
  cancelada
}

enum status_alergia {
  activa
  resuelta
  descartada
}

enum status_condicion_med {
  activa
  resuelta
  cronica
}

enum status_ingreso {
  pendiente
  parcial
  pagado
  cancelado
  reembolsado
}

enum status_det_ingreso {
  activo
  modificado
  eliminado
  reembolsado
}

enum status_pag_ingreso {
  confirmado
  pendiente
  cancelado
  reembolsado
  rechazado
}

enum status_egreso {
  registrado
  anulado
  editado
  reembolsado
}

enum status_evento {
  activo
  finalizado
  cancelado
  oculto
}

enum status_archivo {
  activo
  oculto
}

// ENUMS DE CATEGORIAS PARA CIERTAS TABLAS
enum proveedor_login {
  local
  google
}

enum rol {
  paciente
  dentista
}

enum sexo {
  femenino
  masculino
  otro
}

enum tipo_cobro {
  unidad_anatomica
  plan_terapeutico
}

enum reprog_solicitada_por {
  paciente
  dentista
}

enum severidad {
  alta
  media
  baja
}

// tiene_tutor, evento_todo_el_dia, condicion_controlada, 
enum si_o_no {
  si
  no
}

enum dia_semana {
  domingo
  lunes
  martes
  miercoles
  jueves
  viernes
  sabado
}

// TABLAS


